# إعدادات الخادم (للإنتاج)
# قم بإعادة تسمية هذا الملف إلى .env بعد رفعه إلى مجلد server في الاستضافة

# المنفذ (Port) غالباً يكون محدد تلقائياً من الاستضافة، لكن يمكن تركه
PORT=3001

# رابط الموقع (Frontend) - استبدل هذا برابط موقعك الحقيقي
# مثال: https://my-academy.com
FRONTEND_URL=https://your-domain.com

# بيئة العمل
NODE_ENV=production

# إعدادات قاعدة البيانات (لا تغيرها إلا إذا كنت تعرف ماذا تفعل)
DB_PATH=./database.sqlite

# إعدادات LiveKit للبث المباشر (مطلوبة)
# يمكنك استخدام أي قيم للتجربة، لكن في الإنتاج استخدم قيم قوية
LIVEKIT_API_KEY=devkey
LIVEKIT_API_SECRET=secret
