# تقرير الأخطاء والمشاكل التقنية الشامل في نظام إدارة أكاديمية دارين

تم إجراء مراجعة فنية شاملة للشيفرة البرمجية (Codebase) الخاصة بالنظام (Backend & Frontend) وفحص قاعدة البيانات والوظائف المختلفة. تم رصد المشاكل الأربعة الأساسية بالإضافة إلى مشاكل حرجة أخرى تؤثر على الحسابات المالية والوصول للنظام.

فيما يلي التقرير الشامل المفصل لتقديمه للمبرمج للبدء في الإصلاح الفوري:

---

## 1. مشكلة فشل إعادة تهيئة النظام (System Reset)

### **وصف المشكلة**
عند محاولة تصفير النظام أو إعادة تهيئته من الإعدادات، يفشل الإجراء أو تظهر أخطاء تعارض قواعد البيانات، ولا يتم مسح الحسابات بشكل صحيح مما يمنع استخدام نفس أسماء المستخدمين القديمة مجدداً.

### **الأسباب التقنية (Root Causes)**
1. **عدم تصفير جدول الحسابات الموحد (`accounts`):**
   * تم في تحديثات النظام السابقة إدخال جدول موحد لإدارة تسجيل الدخول يسمى `accounts` (في قاعدة البيانات).
   * في ملف المسار الخاص بالنظام [system.js](file:///d:/dar-edu/دارين%20لتعليم%20و%20التدريب/dareen-app/new-kk/server/routes/core/system.js) (السطر 508 وما بعده)، يقوم كود `system-reset` بحذف البيانات من جداول الطلاب والمعلمين وأولياء الأمور، ولكنه **يتجاهل تماماً** حذف السجلات من جدول `accounts`.
   * يترتب على ذلك بقاء حسابات الدخول القديمة معلقة (Orphaned Logins). وعند محاولة إضافة طالب أو معلم جديد بنفس اسم المستخدم القديم، يرمي خادم قاعدة البيانات خطأ تعارض (`P2002` Unique Constraint) لأن اسم المستخدم لا يزال محجوزاً في جدول `accounts`.
2. **ترتيب الحذف وتعليق المفاتيح الأجنبية (Foreign Key Constraints):**
   * يتم حذف الطلاب والمعلمين قبل حذف جداول الجلسات والفواتير والتقييمات المرتبطة بهم، مما قد يسبب رفض خادم قاعدة البيانات للحذف لحماية العلاقات.

### **الحل المقترح للمبرمج**
تعديل كود معالج `system-reset` في الملف [system.js](file:///d:/dar-edu/دارين%20لتعليم%20و%20التدريب/dareen-app/new-kk/server/routes/core/system.js):
```javascript
// إضافة مسح لجدول الحسابات الموحد وجداول التقييمات والجلسات النشطة قبل مسح الكيانات الأساسية
await tx.account.deleteMany(); // مسح كل حسابات الدخول الموحدة
await tx.activeSession.deleteMany();
await tx.liveSession.deleteMany();
await tx.evaluation.deleteMany();
await tx.teacherAvailability.deleteMany();
await tx.pointsLog.deleteMany();
```

---

## 2. مشكلة عدم ربط المعلمة الجديدة بالطالب (الاشتراكات)

### **وصف المشكلة**
عند فتح كارت الطالب وإضافة "اشتراك جديد" (ربطه بمعلمة وتحديد المادة والجدول)، لا يظهر اسم المعلمة في قائمة الاشتراكات، أو تظهر قائمة الاشتراكات فارغة، أو يفشل الحفظ في الخلفية.

### **الأسباب التقنية (Root Causes)**
1. **خطأ عرض الكائن بدلاً من النص في الواجهة (React Object Rendering Crash):**
   * في ملف الواجهة [StudentDrawer.tsx](file:///d:/dar-edu/دارين%20لتعليم%20و%20التدريب/dareen-app/new-kk/src/features/students/components/StudentDrawer.tsx) (السطر 376)، تتم محاولة طباعة اسم المعلم هكذا:
     `{en.teacher}`
   * في الـ Backend، جلب بيانات الاشتراكات (`enrollments`) يتضمن استعلام ربط الجدول (Include) مع جدول المعلم ليُرجع كائناً يحتوي على تفاصيل المعلم (Object):
     `teacher: { select: { id: true, name: true, subject: true } }`
   * هذا يعني أن `en.teacher` في الواجهة هو كائن (Object) وليس نصاً (String). طباعة كائن مباشرة في React تسبب إما عدم ظهور الاسم أو كراش كامل للواجهة (React Runtime Crash).
2. **اسم العمود الخاطئ في خادم قاعدة البيانات (Prisma Query Type mismatch):**
   * في ملف الخدمة بالـ Backend [studentService.js](file:///d:/dar-edu/دارين%20لتعليم%20و%20التدريب/dareen-app/new-kk/server/services/studentService.js) (الأسطر 129 و 207)، يتم تحديث أو إنشاء الاشتراكات هكذا:
     `teacher: e.teacher`
   * لكن حسب موديل قاعدة البيانات في ملف [schema.prisma](file:///d:/dar-edu/دارين%20لتعليم%20و%20التدريب/dareen-app/new-kk/server/prisma/schema.prisma) (السطر 111)، العمود النصي لاسم المعلم الاحتياطي يسمى `teacherFallback` ومربوط بالاسم المستعار `"teacher"` في جدول قاعدة البيانات.
   * في Prisma، يجب استخدام الحقل `teacherFallback` لتخزين اسم المعلم النصي، لأن حقل `teacher` محجوز كعلاقة (Relation) لنظام الجداول المترابطة، وتمرير نص داخله يسبب خطأ فادحاً في الـ Backend ويمنع الحفظ.

### **الحل المقترح للمبرمج**
1. **في الواجهة [StudentDrawer.tsx](file:///d:/dar-edu/دارين%20لتعليم%20و%20التدريب/dareen-app/new-kk/src/features/students/components/StudentDrawer.tsx) السطر 376:**
   تعديل عرض المعلم ليكون مرناً يقرأ كائن العلاقة أو النص الاحتياطي:
   ```tsx
   <p className="text-[9px] text-muted">
       {typeof en.teacher === 'object' && en.teacher ? en.teacher.name : (en.teacherFallback || en.teacher || 'معلم غير محدد')}
   </p>
   ```
2. **في الـ Backend [studentService.js](file:///d:/dar-edu/دارين%20لتعليم%20و%20التدريب/dareen-app/new-kk/server/services/studentService.js):**
   تغيير `teacher` إلى `teacherFallback` عند إنشاء/تحديث الاشتراك عبر Prisma.

---

## 3. مشكلة ربط وسائل الدفع والعملة وسعر الحصة

### **وصف المشكلة**
النظام لا يقوم بربط سعر الحصة المحدد للطالب والعملة المختارة له عند إنشاء الحصص تلقائياً من الاشتراك، ويحدث خلط كبير في الواجهات بين العملات والمناهج.

### **الأسباب التقنية (Root Causes)**
1. **خلط المبرمج بين العملة والمنهج في نموذج الإدخال (Currency vs. Curriculum):**
   * في ملف المكون [EnrollmentForm.tsx](file:///d:/dar-edu/دارين%20لتعليم%20و%20التدريب/dareen-app/new-kk/src/features/students/components/EnrollmentForm.tsx) (السطر 80)، تمت تسمية الحقل الخاص بالعملة (`curr`) بـ **"المنهج"** ووضع Placeholder يطلب من المستخدم إدخال المنهج الدراسي.
   * ولكن في الـ Backend والـ Database، حقل `curr` يعبر عن **العملة (Currency)** ويأخذ قيمته الافتراضية من عملة الطالب (`student.currency`).
   * نتيجة لذلك، عندما يقوم المستخدم بملء الاستمارة وكتابة "المنهج الكويتي" أو "منهج مصري" في هذا الحقل، يقوم النظام بتخزينه كـ "عملة الحصة"، وتظهر الواجهات المالية والتقارير بأشكال تالفة (مثل عرض "المنهج الكويتي" كرمز عملة في التقارير المالية).
2. **تجاهل سعر حصة الطالب عند توليد الحصص المجدولة:**
   * في ملف [enrollmentService.js](file:///d:/dar-edu/دارين%20لتعليم%20و%20التدريب/dareen-app/new-kk/server/services/enrollmentService.js) (السطر 150 وما بعده)، يقوم النظام تلقائياً بإنشاء سجلات الحصص مجدولة (`sessions`) بناءً على عدد الحصص المطلوب.
   * الكود البرمجي الحالي يقوم بجلب **سعر المعلم الافتراضي** ووضعه كسعر للحصة على الطالب:
     `const price = teacherId ? (await tx.teacher.findUnique({ ... }))?.price || 0 : 0;`
   * هذا يتجاهل تماماً حقل `sessionPrice` (سعر الحصة الخاص بالطالب)، مما يترتب عليه محاسبة الطالب بسعر المعلم وليس بالسعر المتفق عليه والمسجل بكارت الطالب.
   * بالإضافة إلى ذلك، لا يتم حفظ `teacherPrice` (سعر المعلم الفعلي) ولا يتم أخذ لقطة لعملات الطرفين والتحويل بينها، مما يعطل حسابات الأرباح وتسوية فواتير المعلمات تلقائياً.

### **الحل المقترح للمبرمج**
1. **في الواجهة [EnrollmentForm.tsx](file:///d:/dar-edu/دارين%20لتعليم%20و%20التدريب/dareen-app/new-kk/src/features/students/components/EnrollmentForm.tsx):**
   تحديث حقل `curr` ليكون اختياراً للعملة (مثل KWD، SAR، EGP) بدلاً من نص المنهج، وإضافة حقل منفصل للمنهج الدراسي إذا كان ذلك مطلوباً.
2. **في الـ Backend [enrollmentService.js](file:///d:/dar-edu/دارين%20لتعليم%20و%20التدريب/dareen-app/new-kk/server/services/enrollmentService.js) السطر 150:**
   تحديث آلية توليد الجلسات لتأخذ السعر والعملة بشكل صحيح:
   ```javascript
   const price = student.sessionPrice || 0; // السعر الفعلي الذي يدفعه الطالب
   const teacherPrice = teacherRecord ? teacherRecord.price : 0; // السعر الذي تتقاضاه المعلمة
   const studentCurrency = student.currency || 'KWD';
   const teacherCurrency = teacherRecord ? teacherRecord.currency : 'EGP';
   ```

---

## 4. مشكلة عدم وجود بيانات دخول ولي الأمر (اسم المستخدم وكلمة المرور)

### **وصف المشكلة**
لا توجد طريقة لعرض اسم المستخدم وكلمة المرور الخاصة بحساب ولي الأمر، ولا يمكن تعديلها أو إعادة تعيينها عند تعديل بياناته.

### **الأسباب التقنية (Root Causes)**
1. **إخفاء حقول تسجيل الدخول في وضع التعديل بالواجهة:**
   * في ملف استمارة ولي الأمر [ParentForm.tsx](file:///d:/dar-edu/دارين%20لتعليم%20و%20التدريب/dareen-app/new-kk/src/features/parents/components/ParentForm.tsx) (الأسطر 62-65)، تم وضع شرط لإخفاء حقول اسم المستخدم وكلمة المرور أثناء وضع التعديل:
     `{!isEdit && <InputField label="اسم المستخدم" ... />}`
   * هذا يجعل من المستحيل على موظف الإدارة تحديث اسم مستخدم ولي الأمر أو كتابة كلمة مرور جديدة له عند نسيانها.
2. **عدم عرض اسم المستخدم في بطاقة معلومات ولي الأمر (Drawer):**
   * في ملف [ParentDrawer.tsx](file:///d:/dar-edu/دارين%20لتعليم%20و%20التدريب/dareen-app/new-kk/src/features/parents/components/ParentDrawer.tsx) (السطر 125)، تعرض تفاصيل ولي الأمر الاسم والهاتف والبريد الإلكتروني والأبناء، ولكن تم إغفال عرض حقل `parent.username` لتمكين الإدارة من معرفة اسم الدخول الخاص به لتقديمه له.

### **الحل المقترح للمبرمج**
1. **في الواجهة [ParentForm.tsx](file:///d:/dar-edu/دارين%20لتعليم%20و%20التدريب/dareen-app/new-kk/src/features/parents/components/ParentForm.tsx):**
   * السماح بعرض حقل "اسم المستخدم" للقراءة فقط أو التعديل حتى في وضع `isEdit`.
   * عرض حقل "كلمة المرور الجديدة" لوضع كلمة مرور جديدة عند الحاجة، مع تركها فارغة إذا لم تكن هناك رغبة في تغييرها.
2. **في الواجهة [ParentDrawer.tsx](file:///d:/dar-edu/دارين%20لتعليم%20و%20التدريب/dareen-app/new-kk/src/features/parents/components/ParentDrawer.tsx):**
   * إضافة عرض حقل `parent.username` تحت تفاصيل الاتصال لتسهيل الرجوع إليه.

---

## 5. مشاكل حرجة إضافية تم اكتشافها بالتحليل الفني

### **المشكلة أ: حظر دخول أولياء الأمور والطلاب بعد استيراد النسخة الاحتياطية (Restore)**
* **وصف المشكلة:** بعد استعادة أي نسخة احتياطية من الإعدادات، يفاجأ الطلاب وأولياء الأمور بعدم قدرتهم على تسجيل الدخول مطلقاً بهواتفهم.
* **السبب الجذري:** في دالة الـ Restore بالملف [system.js](file:///d:/dar-edu/دارين%20لتعليم%20و%20التدريب/dareen-app/new-kk/server/routes/core/system.js)، يتم استيراد الحسابات وتخطي إنشاء الحساب في جدول `accounts` الموحد إذا كان حقل `username` فارغاً (حيث كان فارغاً بالماضي ويعتمد فقط على الهاتف). غياب سجل في `accounts` يمنع تسجيل الدخول تماماً في النظام الحديث.
* **الحل:** عند استيراد البيانات وإعادة بناء جدول `accounts`، إذا كان حقل اسم المستخدم فارغاً، يجب اعتماده تلقائياً كـ (رقم الهاتف) لولي الأمر أو الطالب لضمان توليد سجل دخول له.

### **المشكلة ب: تجاهل فواتير الطلاب المدفوعة في إحصائيات الأرباح والتقارير المالية**
* **وصف المشكلة:** لوحة التحكم والتقارير المالية تظهر إيرادات غير دقيقة ومختلفة عن الواقع.
* **السبب الجذري:** في ملف الإحصائيات المالية [financeService.js](file:///d:/dar-edu/دارين%20لتعليم%20و%20التدريب/dareen-app/new-kk/server/services/financeService.js) (دالة `getStats`)، يتم حساب الإيرادات فقط من الحصص المنفذة والتحويلات اليدوية، ويتم **تجاهل تماماً** جمع المبالغ القادمة من فواتير الطلاب المدفوعة (`StudentInvoice`). كما أن دفع الفاتورة لا يقوم بتوليد معاملة مالية تلقائية.
* **الحل:** تعديل دالة الإحصائيات لتشمل فواتير الطلاب المدفوعة، أو برمجياً جعل دفع الفاتورة يقوم بإنشاء سجل إيراد تلقائي بجدول المعاملات اليدوية (`ManualTransaction`).

### **المشكلة ج: تحويل طالب المراجعة (Trial Convert) يبتر صلاحيات تسجيل الدخول**
* **وصف المشكلة:** عند تحويل طالب تجريبي (Trial Session) إلى طالب رسمي مقيد، يتم تسجيله بنجاح لكنه لا يستطيع تسجيل الدخول لحسابه الأكاديمي.
* **السبب الجذري:** في معالج التحويل بملف [trial_sessions.js](file:///d:/dar-edu/دارين%20لتعليم%20و%20التدريب/dareen-app/new-kk/server/routes/education/trial_sessions.js) (السطر 97)، يتم إنشاء سجل الطالب بجدول `Student` ولكنه لا يقوم باستدعاء دالة `syncAccount` لتوليد حساب دخول وكلمة مرور افتراضية له بجدول الحسابات الموحد.
* **الحل:** تعديل كود التحويل بالملف لاستدعاء دالة المزامنة `syncAccount` ومنح الطالب كلمة مرور افتراضية (رقم الهاتف مثلاً) وإبلاغ المسؤول بها.

### **المشكلة د: تعارض أسماء المستخدمين المحذوفين (Soft Delete Collision)**
* **وصف المشكلة:** عند حذف طالب أو معلم (حذف ناعم Soft Delete) ثم محاولة إضافة مستخدم جديد بنفس اسم الدخول يرفض النظام.
* **السبب الجذري:** حقل `username` محدد كفريد (`@unique`) في قاعدة البيانات. وبما أن الحذف "ناعم" (أي تظل السجلات موجودة ويتم وضع تاريخ الحذف فقط في حقل `deletedAt`)، فإن قاعدة البيانات تمنع تكرار اسم الدخول وتعتبره مستخدماً، حتى لو كان صاحبه محذوفاً.
* **الحل:** عند تنفيذ الحذف الناعم للطالب أو المعلم، يجب برمجياً إخلاء اسم المستخدم (تعديله ليكون `null` أو إلحاق لاحقة عشوائية به مثل `username_deleted_12345`) لتحرير اسم الدخول للمستخدمين الجدد.

---

### التوجيهات النهائية للمبرمج
يُنصح المبرمج بالبدء الفوري بحل مشكلة جدول الحسابات الموحد (`accounts`) وتحديث دالة `system-reset` ودالة `restore` لحماية النظام من قفل حسابات الدخول، ثم تعديل دالة الربط المالي للاشتراكات وتوليد الجلسات لضمان الفوترة الأكاديمية السليمة.
