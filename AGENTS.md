# AGENTS.md — Dareen Design System v1.0 Migration

## 1. Project Status

| Sprint | Phase | Status |
|--------|-------|--------|
| Sprint 1 | Foundation (Tokens + Theme + Playground + Docs) | ✅ Done |
| Sprint 2 | Brand Validation + UI Validation + Accessibility | ✅ Done (tag: `design-system-v1.0`) |
| Sprint 3A | Shared Components Refactor (Button, Input, Card, Modal, Badge, Alert, Select, Checkbox, Radio, Switch) | ✅ Done — Quality Gate PASS |
| Sprint 3B | Layout Refactor (Sidebar ✅ → Header ✅ → Nav/Tabs ✅ → Breadcrumb ✅ → Footer ✅) | ✅ Done |
| Sprint 3C | Dashboard Widgets (Stats Cards → Charts → Quick Actions → Activity Feed → KPI Cards) | ✅ Done |
| Sprint 3D | Business Pages (Landing → Login → Settings → Students → Teachers → Finance) | ✅ Done — P1=0 |
| Sprint 3E | Parent Pages (ParentDashboard + Parents + ParentAnnouncements) | ✅ Done — P0=0, P1=0 |
| Sprint 4A | Full Project P0 → 0 (568 HEX violations) | ✅ Done — P0=0 |
| Sprint 4A | Full Project P1 → 0 (5004 Named colors via Codemod) | ✅ Done — P1=0 |
| Sprint 4A | Full Project P3 → 0 (108 rgba — 8 CSS-var остаются) | ✅ Done — P3~0 |
| Sprint 4A | P2 Audit: 656→63 (593 replaced with text-on-primary/inverse) | ✅ Done — all remaining justified |
| **Sprint 5** | **Design System v1.1 — Production Ready** | **✅ Done (tag: `design-system-v1.1`)** |
| Sprint 5.1 | DataTable Headless API + StudentTable migration | ✅ Done |
| Sprint 5.2 | Form System (FormField compound + Input/Select/Textarea/Login) | ✅ Done |
| Sprint 5.3 | Dark Mode Primary Family (WCAG AA contrast) | ✅ Done |
| Sprint 5.4 | Skeleton System + Dashboard skeleton loader | ✅ Done |
| Sprint 5.5 | Custom Checkbox/Radio/Switch (RTL + size variants) | ✅ Done |
| Sprint 5.6 | Token split (per-domain CSS files under `src/styles/tokens/`) | ✅ Done |
| Sprint 5.7 | Design System Playground (Skeleton + Dialog + Avatar + Dropdown sections) | ✅ Done |
| Sprint 5.8 | ADRs (4 decisions documented under `docs/adr/`) | ✅ Done |
| **v1.2** | **Design System Polish — Consistency & Components** | **✅ Done (tag: `design-system-v1.2`)** |
| v1.2.1 | Container component + 15 pages migrated from `max-w-[1600px]` → `max-w-page` | ✅ Done |
| v1.2.2 | `focus` → `focus-visible` (keyboard a11y) in Button, Tabs, Modal, Table | ✅ Done |
| v1.2.3 | Motion tokens (`--duration-fast/normal/slow`) + `transition-all` cleanup | ✅ Done |
| v1.2.4 | Elevation System (5 levels: elevation-0 through elevation-4) | ✅ Done |
| v1.2.5 | Color tokens expansion (primary-200/400, text-secondary, border-hover, surface-active) | ✅ Done |
| v1.2.6 | Dialog (Confirm/Cancel), Avatar (first-letter + status), Dropdown | ✅ Done |
| **v1.3** | **Mobile Dashboard UX — Premium Mobile Experience** | **✅ Done (~122 files)** |
| v1.3.1 | Modal → Bottom Sheet on mobile (`< md`), centered on Desktop — all system modals inherit | ✅ Done |
| v1.3.2 | Touch targets ≥44px on mobile: Button, IconButton, ActionRow, Dropdown, FilterDropdown, Dialog, ErrorState retry | ✅ Done |
| v1.3.3 | Table: default mobile cards via `mobileLabel` columns + improved mobileCard styling + pagination targets | ✅ Done |
| v1.3.4 | Tabs `scrollable` variant; Settings 17-tabs → grouped native select on mobile | ✅ Done |
| v1.3.5 | Table split point unified at `md` (Teachers, Leads were `lg` — broken 768–1024 band) | ✅ Done |
| v1.3.6 | FAB unified above AppTabBar: `bottom: calc(96px + env(safe-area-inset-bottom))` (MonthlyClosing, TeacherInvoices, TeacherPaymentHistory fixed) | ✅ Done |
| v1.3.7 | Settings tables (Currencies, AuditLog) → mobile cards; MonthlyClosing date range full-width on mobile | ✅ Done |
| v1.3.8 | Custom centered dialogs → Bottom Sheet on mobile (ConfirmModal, SendNotification, SecureAttendance, Settings×4, AddTransaction, Reschedule, StartLiveSession, Parents×2, Dashboard×4, Schedule×2) | ✅ Done |
| v1.3.9 | Radius unification: `rounded-none` → `rounded-2xl` (73+ files); SalarySlip/InvoicePreview → Bottom Sheet | ✅ Done |
| v1.3.10 | Toolbar restructure (AdminJobs 3-col action grid, AdminContacts/AdminBlog touch chips, TeacherToolbar search text-base) | ✅ Done |
| v1.3.11 | Encoding integrity: byte-level Arabic verification across all 122 modified files (3 BOM files repaired via Node) | ✅ Done |

## 2. Design System Rules (Non-Negotiable)

1. **Components use Semantic Tokens ONLY** — `text-main`, `bg-primary`, `border-border`, etc.
2. **HEX is FORBIDDEN** in components — no `#6C4BFF`, `#10B981`, etc. (exception: `src/theme/` and `src/styles/`)
3. **Tailwind named colors FORBIDDEN** — no `bg-indigo-*`, `text-rose-*`, `border-emerald-*` in components
4. **Palette is NEVER imported inside components** — only imported by Theme layer
5. **Primitives are NEVER imported outside Theme layer** — `src/theme/palette.ts` is the boundary
6. **Status colors** (Success, Warning, Error, Info) are fixed across all themes (ADR-004)
7. **`text-white`/`text-black` discouraged** — use `text-on-primary`, `text-inverse`, etc. Exception: glass effects (`bg-white/80`, `dark:bg-slate-900/60`)
8. **Tailwind opacity modifier** (`/50`, `/20`) doesn't work with hex-based CSS Variables — use named colors for opacity or use a separate class

## 3. Architecture — Token Chain

```
Components (.tsx)
    ↓  (use only semantic token class names)
Semantic Tokens (tailwind.config.js → CSS variables)
    ↓  (mapped to palette colors per theme)
Palette (src/theme/palette.ts)
    ↓  (raw color values)
Primitives (src/theme/primitives.ts)
```

- `src/styles/semantic-tokens.css` — CSS variable definitions for light & dark
- `src/styles/design-tokens.css` — design tokens (spacing, radius, shadows, fonts)
- `src/main.tsx` — imports both CSS files (added in Sprint 3A)
- `src/shared/components/ui/index.ts` — barrel export for all shared components

## 4. Sprint Roadmap

### Sprint 3B — Layout Components
1. Sidebar ✅
2. Header ✅
3. Navigation / Tabs ✅ (Tabs component created + BottomNav refactored + Layout bg refactored)
4. Breadcrumb ✅ (Breadcrumb component created + BlogBreadcrumb refactored + Playground updated)
5. Footer ✅ (PublicFooter refactored — brand colors kept for indigo-950 bg + decorative blurs)

### Sprint 3C — Dashboard Widgets
1. StatCard ✅ (shared component + 8+ inline implementations migrated)
2. Charts ✅ (ChartContainer + ChartTooltip + chart-1..6 tokens + 7 chart files refactored)
3. Quick Actions ✅ (QuickActionsHub + QuickActions + QuickActionsGrid refactored)
4. Activity Feed ✅ (shared ActivityFeed component + executive + recent feed migrated)
5. KPI Cards ✅ (covered by StatCard variant system)

### Sprint 3D — Business Pages
1. Landing ✅ (Home.tsx — 25+ violations → 0)
2. Login ✅ (50+ violations → 0)
3. Settings ✅ (SettingsPage + 20 sub-components → 0)
4. Students ✅ (StudentDashboard + 11 component files → 0)
5. Teachers ✅ (TeacherDashboard + 7 component files → 0)
6. Finance ✅ (FinancePage + 5 component files → 0)

### Audit at end of Sprint 3D
- **P0 (HEX)**: ~146 (remaining in un-migrated files: AdminBlog, Announcements, Appointments, Attendance, etc.)
- **P1 (named colors)**: **0** ✅ — milestone achieved!
- **P2 (text-white/black)**: ~194 (mostly glass effects + legitimate uses on colored backgrounds)
- **P3 (inline rgb/rgba)**: ~45

## 4. Sprint Roadmap

### Sprint 3E — Parent Pages
1. ParentDashboard ✅ (905 lines, ~40 HEX + ~50 named colors → 0)
2. Parents ✅ (194 lines, ~6 HEX + ~8 named → 0)
3. ParentAnnouncements ✅ (240 lines, ~14 HEX + ~20 named → 0)

### Audit after Sprint 3E
All Parent pages clean: P0=0, P1=0

### Completed
- Jul 15 | Dead code cleanup (10 unused files removed): PageTransition, StaggeredList, semantic-tokens.css (migrated to tokens/), App.css (empty), usePageLoader hook, ForumHeader, QuickSearch, MouseGlow, PageContainer, ParentAttendance page (+ 14 empty feature dirs)
- Jul 15 | Second dead code pass (22 more files): AgendaTable, ChatManagement, ImportantNotifications, ParentChildVisualProgress, ParentExcellenceRadar, PerformanceSummary, RenewalAlerts/List, SessionAnalysis, SmartAlerts, TeacherLeaderboard/RewardsKPIs/SalaryPreview/WeeklySummary, dashboardService, ReportsStatsGrid, StudentToolbar, useSharedData (x2), Profile, testUtils, design-tokens.css. tsc + vite build pass.
- Jul 15 | Third cleanup pass: useSettings hook dead (never imported), StudentHeader dead, executive/index barrel dead, VAcademicCap vector dead, json-server dep removed, useToasts/useRequestDesktopNotifications removed, design-tokens.css confirmed dead. tsc + vite build pass.

## 5. Final Audit (Sprint 4A Complete)

| Priority | Baseline (June 2026) | Final (July 2 2026) | Action |
|----------|---------------------|---------------------|--------|
| **P0 (HEX)** | 568 in 61 files | **0** ✅ | Manual fixes |
| **P1 (Named colors)** | 5,004 in 200+ files | **0** ✅ | Codemod scripts |
| **P2 (text-white/black)** | 656 in 126 files | **63** (all glass/decorative) ✅ | All justified (glass effects, opacity) |
| **P3 (rgba)** | 108 in 48 files | **8** (all CSS-var based) ✅ | Acceptable |

### Tooling
- `scripts/codemod-p1.ps1` — P1 replacement script (exact + regex pass)
  - Usage: `.\scripts\codemod-p1.ps1 -Folder "src/folder"`

## 6. Known Issues

| ID | Severity | Issue | Status |
|----|----------|-------|--------|
| P1 | 🔴 High | Dark mode primary contrast (indigo on dark bg) | Deferred to Sprint 4 |
| P2 | 🟡 Medium | Native Checkbox/Radio (no custom styling, uses native `<input type="checkbox">`) | Deferred to Sprint 4 forms refactor |
| P3 | 🔵 Low | RTL border logic in shared components | Deferred to Sprint 4 |
| P4 | 🟡 Medium | `text-on-accent` CSS variable defined in tailwind.config but missing from `semantic-tokens.css` | Fixed in 3C.1 |
| P5 | 🟡 Medium | `text-on-success`, `text-on-warning`, `text-on-info` missing from token chain | Fixed in 3C.1 |
| P6 | 🟡 Medium | Chart color tokens needed (6 distinct chart-safe colors) | Fixed in 3C.2 — `--chart-1` through `--chart-6` |

## 7. Definition of Done (DoD)

Every component MUST pass all checks before being marked complete:

- [ ] **100% Semantic Tokens** — no HEX, no `bg-indigo-*`, no `text-rose-*`, no `border-emerald-*`
- [ ] **Accessibility** — focus ring (`focus:ring-2 focus:ring-focus`), `aria-*` attributes, `role` where needed, keyboard navigation
- [ ] **Dark/Light** — works in both modes via CSS variables
- [ ] **All States** — hover, active, focus, disabled (where applicable)
- [ ] **Responsive** — works at mobile/tablet/desktop
- [ ] **Barrel Export** — exported from `src/shared/components/ui/index.ts`
- [ ] **No Legacy Colors** — zero legacy Tailwind named colors
- [ ] **TypeScript** — clean types, no `any`, no `@ts-ignore`

> **Hard Rule**: أي مكون أو صفحة جديدة يجب أن تستخدم Design Tokens و Semantic Tokens فقط. يُمنع استخدام HEX أو Named Tailwind Colors مباشرة خارج `src/theme/` و `src/styles/`. أي مخالفة تعتبر فشلًا في الـ Code Review.

## 8. Key Decisions Log

| Date | Decision | Rationale |
|------|----------|-----------|
| S2 | `bg-error` light: rose[500]→[600] | WCAG AA contrast 3.67:1 → 4.82:1 |
| S2 | Error hover/active: 600→700→800 | Consistent darkening chain |
| S3A | Added 7 missing tokens to tailwind.config | `on-primary`, `on-error`, `on-accent`, `primary-hover`, `primary-active`, `primary-soft`, `primary-light` |
| S3A | Input/Select/Textarea: auto-generated `id` + `htmlFor` | WCAG label association |
| S3B | Added `main` + `hover` to tailwind.config colors | `text-main`, `bg-hover` used in 3A components but not mapped |
| S3C | StatCard: single component replaces 8+ inline implementations | Reduce maintenance, enforce consistent design |
| S3C | ChartContainer + ChartTooltip as shared infrastructure | Eliminate 437 violations across 12 chart files |
| S3C | Chart tokens (chart-1..6) with light+dark variants | Recharts fill/stroke needs CSS variables, not Tailwind classes |
| S3C | QuickActions: use existing Card+Button+semantic tokens | No new patterns, per user guidance |
| S3C | ActivityFeed: uses Badge variant for status colors | Per user guidance: "عن طريق Alert/Badge وليس ألوانًا خاصة" |
| S4A | P1 Codemod script with explicit map + regex pass | 5,004 named colors → 0 in single automated run |
| S4A | P2 Audit: non-glass text-white → text-on-primary/inverse | 656 → 63 (all remaining justified as glass/decorative) |
| S4A | Migration closed — Design System v1.0 is now a stable platform | No Sprint 5; future work is feature-led |
| Jul 6 | Unused barrel exports removed (Modal, Select, Checkbox, Radio, Switch, Table, Image, Textarea, StaggeredList/Grid) | 12 unused components not in barrel; available for direct import |
| Jul 6 | MonitoringPage refactored: all inline styles → Tailwind semantic | Fixed dark mode compatibility |
| Jul 6 | Button RTL: `ml-/mr-` → `gap-2`; QuickSearch RTL: `ml-4` → `ms-auto me-0` | Direction-agnostic spacing |
| Jul 6 | Codemod: 1,353 `text-[*]` → semantic typography tokens (145 files) | Removed 16 arbitrary pixel size variants; only `text-[6px]` (2) and `text-[var(...)]` (3) remain |
| Jul 6 | Image component: added `imgClassName` prop, exported from barrel, migrated 25/32 native `<img>` → `<Image>` | 7 `<img>` remain inside `<picture>` elements (webp/avif) or raw HTML strings
| Jul 11 | AdminContacts Purity UI reskin — unified card rhythm, semantic tokens, accessibility | First Purity UI migration (commit `9a26f55`) |
| Jul 11 | Leads Purity UI reskin + 4 sub-components (LeadsUI, AddLeadModal, LeadCards, LeadTable) | Unified card rhythm, semantic tokens, accessibility (commit `4e2b979` + QA `cbcdbf0`) |
| Jul 11 | TrialSessions Purity UI reskin — unified card rhythm, semantic tokens, accessibility | Removed glass/gradients (commit `2d7442b` + QA `af22a65`) |
| Jul 11 | Teachers Purity UI reskin — 7 files (TeachersPage, Stats, Toolbar, Table, Form, Details, Card) | Unified card rhythm, semantic tokens, accessibility (commit `2f37466` + QA `c254a08`) |
| Jul 11 | Jobs Purity UI reskin — hero gradients/glass removed, semantic tokens, unified inputs, error modal, success view | Purity UI skeleton (commit `500dc00` + QA `30eb8bc`) |
| Jul 15 | Dead code removal: semantic-tokens.css (4.5 kB), App.css (empty), PageTransition, StaggeredList, usePageLoader, ForumHeader, QuickSearch, MouseGlow, PageContainer, ParentAttendance page | 10 files deleted, tsc --noEmit passes; cleaned up empty feature directories |
| Jul 15 | Second cleanup pass: 22 unused files deleted; design-tokens.css removed (content lives in tokens/); public/sw.js restored (dynamically registered) | Cleanup round 2, tsc --noEmit + vite build pass |
| Jul 15 | Third cleanup: 3 dead files deleted, VAcademicCap removed, json-server purged, 2 unused hook exports removed | Cleanup round 3, all verified |
| Jul 19 | Typography cleanup: `text-[color]` corrected in 4 files (Home, ExecutiveDashboard, BlogHero, About), `tracking-[0.2em]`→`tracking-label` in 14 files (18 occurrences), `leading-[*]`→Tailwind leading tokens in 7 files, `design-tokens.ts` font family mismatch fixed | `tsc --noEmit` passes |
| Jul 19 | Gradient cleanup: `from-[var(--bg-*)]`→`from-*`, `to-[var(--bg-*)]`→`to-*`, `via-[var(--bg-*)]`→`via-*` across 24 files (HeroSelection, SelectionGrid, CollectionsTable, SalarySlipModal, InvoiceTables, HowToSubscribe, AboutCTA, Contact, AboutValues, Blog, MonthlyClosing, MasarSection, MobileHeader, PublicNavbar, PageLoader, AdminHomeTab, Home, Courses, TermsOfService, TermsOfWork, RefundPolicy, WhyChooseUs, BlogPostShareSection, HowItWorks) | `tsc --noEmit` passes |
| Sep 4 | Mobile Dashboard UX (v1.3): ~122 files — Modal/Dialog → mobile Bottom Sheet, ≥44px touch targets, unified `md` table split, FAB above AppTabBar, Settings grouped select, mobile cards for Currencies/AuditLog, radius unification | `tsc` + `vite build` + 173 tests pass; Arabic byte-integrity verified on all modified files |
| Sep 5 | Dashboards pass 1 — unified `DashboardGreeting` hero (Teacher/Student/Parent) + admin greeted by name in Executive header | 4 components delegate to shared component; `tsc` clean |
| Sep 5 | Inner-pages pass — `ParentsHeader`/`ParentsStudentHeader` → `PageHeader`; dead `avgChildren` removed; TrialSessions header wrapped in card; FAB unified above AppTabBar (Attendance/Tasks); 44px touch targets (TrialSessions pagination, EvaluationsHeader, TeacherInvoicesHeader) | Consistency targets: PageHeader for table pages, gradient hero for context pages, stat-card hero for finance pages |
| Sep 6 | Shared `DashboardSectionCard` (teacher desktop+mobile `SectionCard` dedup) + `GradientHeroCard` (Attendance/Appointments/Schedule/Forum gradient heroes unified) | Removes ~160 duplicated lines; single source for card/gradient hero chrome |
| Sep 6 | RTL fixes — status dots `-left/-right` → `-start/-end` (LiveSessions, ExecutiveAlerts); Student dashboard micro-typography raised one step (8–10px → 9–11px) | Physical-side utilities break RTL mirroring; readable label hierarchy |
| Sep 6 | Micro-typography pass 2 — zero `text-[7px]` project-wide; document labels 8px → 9px (Parent/Student drawers & tables, ScheduleGrid, FinanceCharts/Stats, AttendanceHistoryModal, Executive chips), 9px → 10px (admin QuickActions/Focus/FinanceOverview) | Full-project hierarchy 9–11px labels vs ≥12px titles; 8px kept only in notification badges/tooltips/24px avatars |
| Sep 6 | Keyboard focus pass — `outline-none focus-visible:ring-2 focus-visible:ring-focus` added to every interactive element lacking it: shared Table (pagination + both mobile-card row variants), MobileListItem/MobilePageHeader/MobileTopBar/BottomSheet, Confirm/Success modals, SectionErrorBoundary, LiveSessions join link + QuickActions desktop cards (manual) then codemod across 35+18 files (53 total); input `focus:ring-primary/10` border rings deliberately untouched | DoD a11y — Tab-navigable elements must show a visible ring; `:focus-visible` (not `:focus`) avoids mouse-click rings; text inputs keep their instant `:focus` border cue |
| Sep 6 | Empty-state copy unification — one recipe `text-xs font-bold text-muted` (primary) + `text-xs text-muted` (detail); arbitrary `[9px][10px][11px][13px]` raised to `text-xs`; `text-main`→`text-muted` quieting; redundant `dark:text-muted` dropped; 24 files / 40 changes; 7 deliberate exceptions kept (icon-hero empties ≥32px, GamificationCard stamp style, NextSessionRadar emphasis); BOM stripped in 4 files | 26 different inherited/legacy classes on empty lines → one readable tier (12px bold muted); arbitrary pixel sizes violate hierarchy; BOM removal aligns with Sep 4 encoding work |
| Sep 6 | RTL logical-utility sweep pass 2 — Toast stack `left-4`→`start-4`, TrialSessions desktop FAB `left-8`→`end-8`, AdminJobs status accent `border-r-4 border-r-*`→`border-s-4 border-s-*`, Sheet footer `space-x-2`→`gap-2`; verified `border-s-success`+`border-s-primary/40` generate in build; audited 1163 physical utilities — the rest are symmetric `px-*`/`mx-*`, chart-axis margins, centered tooltips `left-1/2 -translate-x-1/2`, or JS props | RTL-first app: logical inset/border utilities mirror correctly if any LTR page appears; visual identical in Arabic (me/ms/start/end = physical right in RTL); Input already used `start-3`/`end-2`/`ps-10`/`pe-12` |
| Sep 6 | Card-title weight unification — `text-sm font-bold text-main` → `text-sm font-black text-main` for every card/section title (executive dashboards, finance panel titles, settings sections, Roles, FinancialReport, shared ActivityFeed + profile header, Jobs font-heading titles), 18 files / 18 lines; `dark:text-main` dups dropped (FinanceOverview, TeacherSessionTimeline); legitimately left `font-bold`: modal-title family, entity/student names, colored `on-primary`/`on-error` band headers, content titles (announcements/blog posts) | Sanctioned tier from shared components: card titles = `font-black text-sm` (DashboardSectionCard, GradientHeroCard), page titles = `font-bold` (PageHeader), numerals inherit `font-dash` wrapper — one visible weight inconsistency across ~30 card titles eliminated |
| Sep 6 | Contrast audit on colored fills — `--bg-success` light emerald[500]→[600] (palette.ts + colors.css; dark was already 600): white text on success fills was 2.5:1 (failed even 3:1 bold) → now 7.3:1 AA, matches darker status-fill precedent (S2 error 500→600); secondary text on primary/gradients bumped `text-white/70`→`/90`, `/65`→`/90`, `/60`→`/80` across ~24 internal sites (hero chips, modal band labels, toast/card text) — 3.9:1/3.3:1 → ≥4.9:1 AA; `--chart-2` stays #10b981 (chart token, not a text fill); icons (glyphs, buttons) left at original opacity (decorative icons exempt from WCAG text contrast), Login/`dark:`-guarded copies also raised — one uniform rule | WCAG AA normal-text 4.5:1 validated numerically against palette luminance — token-layer fix repairs every success surface at once (charts untouched) |
| Sep 6 | Grayscale text contrast — `--text-dim` raised in light `#94a3b8`→`#66778e` (2.56:1 → 4.57:1 AA) and dark `#52525b`→`#7b7b85` (2.51:1 → 4.64:1 AA), matching approach scaled: light `--text-muted` stayed #64748b (already 4.76), dark `#71717a`→`#8b8b93` (4.02 → 5.74); `desktop-light` hero block got the same dim raise; palette.ts `textDim` synced from `slate[400]` to the working literal (semantic.ts reads it) | Muted/secondary text must clear WCAG AA 4.5 on card; slate400 is mathematically too light on white — any non-main text under 4.5 is now removed across light/dark/{desktop-light} |
| Sep 6 | Dead hover-token audit — `warning.hover` in tailwind.config resolved to `var(--bg-warning-hover, var(--bg-warning-dark))` where **both** were undefined → warning buttons (BlogCard, ParentDrawer) and `from-warning to-warning-hover` gradients (StudentDrawer) had a silent no-hover/no-gradient-stop; added `--bg-warning-hover` light `#92400e` (amber800, darkening chain from amber700, white 7.1:1) + dark `#d97706` (amber600, lighten-on-dark matching error-hover logic); verified success/error/info hover tokens all resolve (success via `--bg-success-dark` fallback, error explicitly, info explicitly) | Every `*-hover`/`*-active` color key used in components must resolve to a defined CSS var/first-valid fallback — silent CSS failures are invisible regressions on interactive feedback |
| Sep 6 | Legacy `focus:` ring pass (38 files / 242 rings) — every gold `ring-focus` keyword ring converted to `focus-visible:` (`focus:ring-2 focus:ring-focus`→`focus-visible:ring-2 focus-visible:ring-focus`, `focus:outline-none`→`outline-none`, `dark:` variants too), killing the mouse-click ring on controls (Tabs, Modal, ConfirmModal, Badge, Forms/Inputs, Roles, Finance modals, BlogForm family, JobsFields, AdminContacts/AdminJobs, Login, StudentsPage); **deliberately kept `:focus`**: tinted input cues (`focus:ring-primary/10`, `primary/50`, `border-primary`, validation `error/warning/success` soft rings) and `focus:ring-ring` (badge) — mouse-visible instant border cue on text inputs is the sanctioned DS pattern; blog components excluded | Same a11y rule as the Sep 6 keyboard-focus pass — keyboard rings must show, mouse rings must not (WCAG 2.4.7 rendered via `:focus-visible`); text inputs keep their familiar `:focus` border feedback even for mouse users |
| Sep 6 | Contrast audit part 2 — `--bg-warning` + `--bg-info` light and dark unified to amber[700]/sky[700] (palette.ts + colors.css; were amber500/sky500 light + amber600/sky600 dark): white/`text-warning-foreground` on them was 1.5–2.8:1 (failed even 3:1) → now 5.0:1 / 5.9:1 AA; light `--bg-info-hover` → sky[800] (7.6:1), dark hover sky[600] (dark-mode lighten logic); soft/dark-text pairs (`bg-warning-soft text-warning`, `bg-info-soft text-info`) and gradients untouched — verified zero literal `bg-{warning,info} text-{warning,info}` broken pairs app-wide | Same invariant as S2 error → emerald600 success: any `bg-<status>` + `text-on-<status>` pair must meet AA in both modes; warning/info joins error/success on the darker-fill standard |
| Sep 6 | Typography phantom-tier pass — `text-[13px]` (33 sites) was a hidden duplicate of `text-sm` (`--font-size-sm: 0.8125rem` = 13px), `text-[12px]` = `text-xs`, single `text-[14px]` (TrialSessionCard title) dropped to `text-sm`, `md:text-[15px]` (Header) → `md:text-base` — 17 files, zero visual change except 1px on two lone sizes; raised 3 off-doctrine `text-[8px]` notes to 9px (ParentDrawer/ParentsTable overflow +N, StudentTable remaining-balance); remaining `text-[8px]` = badges/tooltips/24px-avatar initials only | Hardcoded arbitrary px that collides with an existing token is a silent tier-split — `text-sm` is 13px in this DS (not 14px) so 13px was the token all along; 8px text outside badges/tooltips/avatar-initials fails the 9-11px label doctrine |
| Sep 6 | Radius arbitrary-value pass — zero `rounded-[Npx]` left in internal components (7 sites → tokens): chat bubbles `rounded-[12px]`→`rounded-lg` (12px exact), AppearanceSection swatch `[10px]`→`rounded-md` (10px exact), InstallPWA toast `[20px]`+icon tile `[14px]`→`rounded-2xl` (16px card), AppTabBar floating pill `[26px]`→`rounded-full` (cap ≈ half of 60px bar = pill intent), StudentDrawer mini swatch `[3px]`→`rounded` (DEFAULT 4px), LeadsUI sparkline `gap-[2px]`→`gap-0.5` (2px exact); `rounded-xs` does NOT exist in the default scale — avoided | The DS radius ladder is `rounded`(4)/`sm`(8)/`md`(10)/`lg`(12)/`2xl`(16)/`full`; arbitrary radii digitize the intent into a token — pillow radii ≥16 fold up to `2xl`, near-full caps to `full` |
| Sep 6 | Redundant `dark:` pass — 88 identical-token pairs removed (16 files): `text-main dark:text-main`, `text-muted dark:text-muted`, `text-dim dark:text-dim` etc. on the same className (CSS-var tokens auto-flip in `.dark`, so the dark variant is a no-op subclass); **DELIBERATE `dark:` overrides kept**: glass (`bg-white/80`, `bg-white/10`), white-text-on-fill (`text-white/80` → `dark:text-muted`), different-token swaps (`bg-surface` → `dark:bg-card`/`dark:bg-hover`, `bg-background`→`dark:bg-surface`), hover chains (`hover:bg-hover` + `dark:hover:bg-hover`), status swaps (`bg-warning-soft`→`dark:bg-primary-soft`) — an earlier membership-by-keyword codemod wrongly hit glass lines and was fully restored from HEAD; only the adjacent-literal (string-boundary) matcher is trusted | Removing a `dark:X` variant is only guaranteed safe when the SAME className carries the bare token `X` ADJACENTLY (identical by definition); membership checks via `\bkeyword\b` corrupt glass/override lines because `:bg-card` embeds a word boundary — verified 88/88 removed lines are pure duplicate pairs, zero glass/swap lost |
| Sep 6 | Compliance re-audit (post v1.3 polish, 7 rounds) — green platform: **P0 (HEX) = 0** and **P1 (named colors) = 0** across every `.tsx` component (styles/theme/blog paths excluded at the layer they allow); P2 `text-white/black` = 102 all on-fill/glass/icon-fills (icon glyphs on primary/status tiles, gradient-hero captions, status-swap `text-on-success dark:text-white`); P3 `rgba(...)` = 4 — all `rgba(255,255,255,0.18)` decorative strokes on gradient heroes (glass, sanctioned); mobile overflow audit: `min-w-[1000px]` ScheduleGrid sits inside `overflow-x-auto`, decorative 400–520px blur blobs all clipped by `overflow-hidden` parents — zero 360px-break risks | The token-chain invariant holds after every polish round: components-only-audit (excluding the token layer) is the trusted definition of P0/P1 = 0; every remaining P2/P3 site must name its waiver (fill glass icon decorative-gradient) at re-audit time |
| Sep 6 | Dead `dark:hover:` pass — 15 hover duplicates removed (8 files): `dark:hover:bg-hover` where the line also carried light `hover:bg-hover` (+ `dark:bg-hover`), `dark:hover:bg-error-soft`/`bg-success-soft`, `dark:hover:text-main`, `dark:hover:border-primary/30`, ParentDrawer status buttons `dark:hover:bg-{success,warning,primary,error}-hover`, IconButton `dark:hover:bg-error-soft dark:hover:text-error` — light `hover:X` already flips the CSS var in `.dark`, so a `dark:hover:X` twin is a full no-op; **KEPT (meaningful)**: two-value swaps (`hover:bg-black/5` → `dark:hover:bg-white/5`, `hover:bg-surface` → `dark:hover:bg-card`/`bg-hover`, `hover:bg-error-light` → `dark:hover:bg-error-soft`, `hover:bg-primary-soft` → `dark:hover:bg-primary/10`, `hover:border-primary/30` → `dark:hover:border-border`), status hover chains with different dark token, and unique dark hover states | Same invariant as the dark: pass — `dark:hover:X` is Safe only when `hover:X` (same token) rides the same className AND `X` is a theme-flipping var (never black/white/opacity/arbitrary); 15/15 verified identical, 26 kept as real dark-mode hover designs |
| Sep 6 | Spacing/chip token pass — `min-h-[36px] min-w-[36px]` → `min-h-9 min-w-9` (LiveSessions icon tiles, decimal exact), `h-9 min-w-[36px]` → `h-9 min-w-9` (Table pagination), off-scale 34px action chips → `min-h-9 min-w-9` (36px, +2px unify with neighboring h-9 controls; StudentTable ×6 + TeacherTable ×4), `w-[2px]` decorative timeline hairline → `w-0.5` (ActivityFeed), `min-w-[2.5rem]` → `min-w-10` (ProgressBar counter), duplicate `min-w-[28px] min-h-[28px] w-7 h-7` → `w-7 h-7` (TeacherEnrollmentList, redundant triple); **kept off-scale**: 30px avatar tiles (NotificationsCenter, between w-7/w-8 = graphic choice), 22–28px display numerals under `font-dash` (HeroSection/MoneyStrip/DashboardStats = intentional display ramps) | Chip/tile sizes must ride the spacing scale when exact (`w-9`=36, `w-8`=32, `w-7`=28); off-scale fixes (34px) fold UP to the next step for touch uniformity; display numerals + avatar tiles stay off-scale by design (font-dash ramp / 30px avatar graph) |
| Sep 6 | Duration-token pass — 190 raw durations in 95 files migrated to motion tokens: `duration-200`→`duration-normal` (200ms), `duration-300`→`duration-slow` (300ms), `duration-150`→`duration-fast` (150ms) — all values already defined as `--duration-*` in the token layer, so zero visual change; **kept**: `duration-500` (sheet/InstallPWA entrance, toast autodismiss) and `duration-700` (progress-bar fill growth: MonitoringPage, FinancialSnapshot, AcademicReport/AttendanceReport/ReportStudentTable, ModernAnnouncements, TeacherAchievements, TopAttendanceStudents) — no 500/700 tokens exist and these are deliberate long-ramp animations; MotionSection playground left untokenized (design-system layer) | Raw `duration-Npx` numbers that collide with a semantic motion token are the same phantom-tier malady as `text-[13px]` — numeric duplicates hide the intent; long ramps (sheets/entrance/progress-fill) are the sanctioned out-of-token uses |
| Sep 6 | RTL logical-utility sweep pass 3 — audited all physical utilities in internal components: `text-right`→`text-start` (7, TrialSessionCard — RTL-identical, LTR-safe); verified **kept w/o change**: AttendanceStats caret `border-l-4 border-r-4` (symmetric diamond, direction-neutral), ParentsTable avatar stacks `-space-x-1.5 space-x-reverse` (correct RTL overlap), `space-x`=0 other; `ease-*` raw = 10 total but all native Tailwind defaults (no timingFunction map exists — nothing to tokenize); icon-button accessible-name audit = 0 unnamed buttons (every raw button carries aria-label/title/text); `transition-all` = 549 left deliberately — progress-bar fills animate width (bare `transition` drops width) so bulk swap would break them | Physical alignment utilities (`text-right`) and Tailwind flex `space-x` break mirroring when any LTR page appears / RTL gets jank — logical `text-start/gap` is the only safe form; the avatar stack's `-space-x-reverse` pattern is the sanctioned exception (purposeful overlap); lazily re-audited: zero real directional hazards remain |
| Sep 6 | Interactive-behavior audit — GREEN: `href="#"` dead = 0; raw `<img>` missing `alt` = 0; `<button>` without `type` = 443 but **all outside `<form>`** (default submit is inert per spec), 18 `<form>` elements × 15 explicit `type="submit"` → zero implicit-submit hazards (no accidental reload); blanket `type="button"` retrofit declined (would break forms that legitimately rely on implicit submit / 443-line noise with zero gain) | Three predicate smells measured to zero/verified-benign before touching anything: dead hash targets, unlabeled images, implicit form submitters — the common "just add type=button everywhere" codemod is a form-submit breakage trap; per-spec benign defaults were confirmed instead |
| Sep 6 | Dark-mode white-patch audit — GREEN: 13 solid `bg-white`/`text-white` sites lack a same-line `dark:` override but all are white-on-colored-fill (mode-invariant): toggle-switch knobs (AppearanceSection/SettingsUI/TeacherForm), live-session pulsing dot + glass chips (ActiveSessionBanner), progress fills on branded strips (GreetingStrip), check marks on colored tiles (AppearanceSection/EvaluationFormModal), icon+label tiles and glass search (SchedulePopover/ForumHeader), ternary tab pill (MobileSchedule); zero white blocks on neutral surfaces = no dark-mode break | Same invariant as the P2 waivers — white glyphs/tiles are safe when the FILL stays colored in both modes; the audit target was white-on-NEUTRAL surfaces (would look like a light-mode hole in `.dark`) and none exist |
| Sep 6 | Layout-mechanics unification — `space-y`×398 audited per same-className-expression scope (flex-col/grid must be IN the same `className=`/`cn(` block, else checking above line falsely catches parent containers): only 5 verified flex/grid containers converted `space-y-N`→`gap-y-N` (SheetHeader, CardHeader, SecureActionModal, MonthlyReportPreview, TeacherStudentCard — zero visual, single flex mechanism); **kept** 393: `space-y` is the correct Tailwind idiom on plain block-flow containers where `gap-y` doesn't apply — 375 provably so + the fractional `0.5/1.5` steps; `space-x`=4 all sanctioned avatar stacks, `ml-/mr-`=0, `mx-`=162 all symmetric (RTL-safe) | Same invariant as RTL sweeps — the container must prove flex/grid in the SAME class expression to convert; Tailwind `gap` only works on flex/grid, so blanketing `space-y`→`gap-y` across 393 block-flow stacks would silently collapse their spacing; a "space-y is legacy" codemod is a layout-collapse trap |
| Sep 6 | Clickable-affordance pass — 28 interactive elements got hover/active feedback: native `appearance-none` selects → `hover:border-primary` (filters, editors, invoicing pickers: AttendanceFilters/AppointmentsFilters/AppointmentFilters, TeacherForm select, StudentInvoices, TeacherPaymentHistory, ScheduleEditorModal, TaskFormModal), transparent text selects/buttons → `hover:text-primary` (TeacherInvoicesHeader/AttendanceHeader/SessionsModal), MobileAttendance text selects → `hover:text-primary`, hero date chip → `hover:text-white/80`, accordion/collapsible rows → `hover:bg-hover` (TransactionsLog), toggle/checkbox rows → `hover:opacity-80` (BlogForm/BlogFormSeoSection/TeacherForm/InvoicePreviewModal), LeadCards → `hover:shadow-elevation-2`, LeadTable sort chip → `hover:bg-hover hover:text-primary`; **covered/kept**: transparent inner selects (wrapper got border hover), TrialSessionCard inner click area (card root already `hover:shadow-elevation-1`), native checkbox (own affordance), `cursor-pointer` on-to-focus rows; semantic `bg-status`+`text-on-*` pair audit = 0 wrong pairs | Any element promising clickability (`cursor-pointer`) must visibly react on `:hover` — pointer without visual response is broken affordance; native selects get the same border cue language as focus rings; status fills pair ONLY with their own `on-*` token |
| Sep 6 | Clickable-affordance pass 2 — `cn(` multi-arg sweep (the earlier pass only scanned single-quoted `className="…"`): 1 more site fixed — shared `MobileListItem` rows got `hover:bg-hover` (already had `active:scale-[0.985]`, role/onKeyDown keyboard a11y, focus ring); full-project `cursor-pointer` + no-hover residual = 0 (4 kept sites documented as covered by wrapper/root/native affordance); production `vite build` + tsc verified new hover utilities (`hover:border-primary`,`hover:shadow-elevation-2`,`hover:bg-hover`,`hover:text-primary`,`hover:opacity-80`,`hover:text-white/80`) actually generate in dist CSS | Text-input/select hover rings were previously deliberately left minimal (focus-only) — the affordance contract now reads: pointer ⇒ `:hover` reaction exists on every interactive surface; shared components carry it permissive by default |
| Sep 6 | Raw-input accessible-name pass - 29 Arabic aria-labels added to unnamed text/date/time/password inputs in 14 files (RescheduleModal date+time, AddTransactionModal amount/category/date, TransactionsLog search, MaintenanceModal confirm-word, PasswordConfirmModal, PermissionsSection role, StudentsPage delete-all password, BlogForm category/source/fileSize, ForumPostCard edit-comment x2, LeadDrawer phone/subject/curriculum, InvoiceForm teacher/specialization/amount/payment/expenses, TrialSessionFormModal 5 fields, TrialSessions delete confirm, ConfirmModal password gate); scanner verified residual raw unnamed inputs = 0 (label-wrapped checkboxes are named, shared Input/InputField composites carry FormField auto-id+htmlFor, EvaluationFormModal points + Advertisers URL already named) | Raw multiline JSX tags evade [^>]* line-scans (an onChange={...}> swallows the closing >), and composite InputField tags false-positive a <input prefix regex - windowed multi-line scan checking id/aria-label/name within +-18 lines is the trusted detector; every a11y audit must re-run it after edits (10 raw hits post-fix were all <InputField/<InputInner composites, zero real) | tsc + 173/173 tests pass; UTF8 round-trip clean on all 14 files
| Sep 6 | Real-names pass - 59 Arabic aria-labels on unnamed/placeholder-only form controls in 24 files: definition-level dynamic (ParentForm InputField aria-label={label} covers 4 fields); InputField/TextAreaField raw usages (AcademicYearSection 5, AttendanceSettingsSection 3, GeneralSettings 9 + currency select, PoliciesSettings, RewardsSettingsSection 5, WorkingHoursSection 5 incl. single-line breakEnd, AcademyInfoSection 6, AdvancedSettings 4, CommunicationsSection, CurrenciesSection 5, FooterSettingsSection 3, MobileSettings 2, ReportsSettingsSection 2, SecureActionModal, UsersSettings 2); raw textarea without associated label (ForumPostCard edit-post, LeadDrawer notes, RescheduleModal reason, AddTransactionModal desc, TrialSessionFormModal notes, TeacherStudentCard notes, SchedulePopover note); ScheduleHeader filter selects x3; fixed reply-input miss from prior round (indent mismatch defeated replaceAll); name-attr inputs (Leads AddLeadModal, JobsInputField) + label-wrapped Advertisers URL already named | Placeholder is a weak fallback name, and a sibling <label>/FieldLabel without for=/aria-label leaves the control unnamed - subtle rap: <input{...}> definitions (SettingsUI) and name-attributed inputs (JobsInputField/Leads) pass a same-line scan; PS -match is CASE-INSENSITIVE so <textarea matched <TextArea in detection; a verified block scanner walking to the real closing /> (arrow => in onChange swallows a naive [^>]*>) is the only trustworthy detector | tsc + 173/173 tests + UTF8 round-trip clean on 24 files
| Sep 6 | Elevation ladder completion + native-shadow merge - --elevation-0 (0 0 #0000) + --elevation-4 (== old --shadow-xl) added to elevation.css and registered in tailwind.config boxShadow, both previously DEAD classes (shadow-elevation-0 on TrialSessionCard/TrialSessions rest-cards, shadow-elevation-4 on Toast hover); merged the duplicate native ladder with zero visual change: shadow-sm/md/lg/xl -> shadow-elevation-1/2/3/4 across 182 files (546 replacements, drop-shadow-lg guarded via (?<!drop-) lookbehind for the 2 Home.tsx sites); kept documented: bare shadow (DEFAULT), shadow-2xl (modal depth), shadow-soft, shadow-card, shadow-gold, shadow-glass, 13 neo-brutalist arbitrary shadow-[N_black] - all unique branded values; FIXED latent tailwind-merge bug surfaced by the rename (Badge glow test): custom shadow-elevation-N fell into the shadow-color isAny group, so pairing shadow-elevation-1 + shadow-primary/10 silently dropped the elevation; cn() now built from extendTailwindMerge overriding the shadow scale group (''/inner/none/elevation-0..4 + isTshirtSize + isArbitraryShadow) and shadow-color group (isAny); verified merge semantics: scale+color coexist, scale-only last-wins, hover: variants untouched | A used utility class must resolve to a defined token (silent dead classes = invisible regressions); custom shadow tokens MUST be registered in tailwind-merge's shadow scale group or they collide with the shadow-color group and eat each other - utils.ts cn() is now the single sanctioned home for that class-group mapping | tsc 0, 173/173 tests, vite build confirms elevation-0/4 rules now generated in dist CSS, UTF8 round-trip clean on all 182 files
| Sep 6 | Token-chain integrity re-audit - walked all 129 var(--X) referenced in tailwind.config.js against every var declared in src css: found & resolved 4 dangling references - (a) added --bg-warning-dark (light #78350f / dark #f59e0b) + --bg-info-dark (light #0c4a6e / dark #38bdf8) to colors.css so the hover fallback chains (hover: 'var(--bg-X-hover, var(--bg-X-dark))') resolve fully; (b) removed the dead 'ring-offset': 'var(--ring-offset)' color key (var never declared, class never used - silent trap for ring-offset-ring-offset); (c) added color keys 'ring': 'var(--ring-focus)' + 'primary-foreground': 'var(--text-on-primary)' which fix the LIVE legacy shadcn checkbox.tsx (RolesPage is its only importer) classes 'ring-ring' + 'data-[state=checked]:text-primary-foreground' that previously generated NOTHING (ring fell back to Tailwind default blue, check glyph inherited parent color); verified in dist CSS: .ring-ring emits --tw-ring-color: var(--ring-focus), .data-[state=checked]:text-primary-foreground emits color: var(--text-on-primary); legacy src/components/ui sheet.tsx/badge.tsx/button.tsx confirmed dead (no importers) | Any color key used by a class MUST exist in the config palette - a missing key silently drops the whole rule (Tailwind emits nothing, browser falls back to defaults); every fallback var(, ) in a multi-value var() must resolve (same rigor as the dead hover-token audit) - a re-audit pass should re-run the src-wide var census after any palette/config change | tsc 0, 173/173 tests, vite build confirms generation + --ring-offset absent, UTF8 round-trip clean on colors.css + tailwind.config.js
| Sep 6 | cn() shadow-shape scale guard + merge regression test suite - audited inline style={} literals (extended the P0/P3 className audit to the style-prop channel): zero HEX app-wide, only 2 built-in rgba(255,255,255,0.1) newsletter/grid hairlines on gradient heroes (DesktopLibraryLanding:411, HeroSelection:325) - sanctioned decorative white strokes; then found a LATENT tailwind-merge landmine: default twMerge classifies every custom shadow token (elevation-N, soft, card, gold, glass, broad) as shadow-COLOR (isAny), so any scale+shadow-primary/20 pairing silently DROPPED the scale (default twMerge('shadow-elevation-2 shadow-primary/20') = 'shadow-primary/20'); our extendTailwindMerge already registered elevation-0..4 as scale, but soft/card/gold/gold-hover/glass/broad fell in the color group too - added them to the shadow scale list in src/lib/utils.ts (all 12 probes now keep both classes); verifier proved zero CURRENT collisions app-wide (4 same-line multi-shadow sites were all benign: shadow-[var(--shadow-*)] arbitrary classes + their embedded CSS var name) - the fix is a pure future-guard; NEW src/lib/utils.test.ts pins 4 merge contracts (scale+color coexist; scale-vs-scale last-wins; hover: variant independence; branded scales like soft/card/glass/gold included) so the elevation-Badge bug class is regression-guarded | The DS shadow ladder's custom tokens live in TWO groups - a scale token must be registered in the shadow SCALE group BEFORE tailwind-merge's isAny color fallback eats it; audit = count same-className shadow pairs, but ignore the CSS var name embedded inside shadow-[var(--X)] arbitrary classes (regex must strip --shadow-* before counting) | tsc 0, 177/177 tests (4 new), vite build passes, UTF8 round-trip clean on utils.ts + utils.test.ts
| Sep 6 | Spacing arbitrary-px detail pass (codemod) - zip-line audit of all w/h/min-w/min-h/p/gap -[Npx] in src (1100 sites) pruned to the EXACT matches of the 4px Tailwind ladder only: h-[1px]→h-px, h-[8px]→h-2, h-[36px]→h-9, h-[44px]→h-11, h-[48px]→h-12, h-[56px]→h-14, h-[80px]→h-20, w-[48px]→w-12, min-h-[8/36/44/80px]→min-h-2/9/11/20, min-w-[14/16/20/32px]→min-w-3.5/4/5/8 (42 replacements in 30 files, 25 net); CATCH of the same class as the phantom-tier rule: 52px/60px/72px have NO default keys (13/15/18 absent from the spacing scale - it jumps 12→14→16 and 16→20) so their h-13/h-15/min-h-15/pt-18 conversion drafts were REVERTED to arbitrary before committing (invalid classes generate NOTHING, the classic silent-dead-class); also fixed the one escaped text-[13px] duplicate (DesktopLibraryLanding:237, redundant next to lg:text-sm); conscious keeps: >96px graphic/hero/video sizes, 9-11px micro-ladder (1381 sites), 8px badges, 30px avatars, distinctive plural hairlines (h-[3px]/h-[5px]) | A px-to-class mapping is only valid if the TARGET key exists in tailwind's default spacing dictionary - a regex map must be validated against the official key list (not by math), and every generated selector must be re-confirmed in dist CSS; the negative lookbehind (?<![a-zA-Z0-9-]) prevents min-h- corruption; md:-prefixed variants and CSS-escaped classes (.min-w-3\.5) both confirmed in dist | tsc 0, 177/177 tests, vite build generates all (incl. md\\:min-w-8), UTF8 round-trip clean on 25 files
| Sep 6 | Spacing-family completion + exact-ladder closure - remaining inset/height collisions in the logic-sized families (>96px the padding pass had kept behind graphic rule): re-audit with the official key map found 12 EXACT ladder sites that DO have valid keys (the 'keep >96' waiver only protects unkeyed sizes like 500/520/600/1400px): h-[240px]→h-60, w-[240px]→w-60, min-w-[160px]→min-w-40, min-w-[240px]→min-w-60, max-h-[320px]→max-h-80, h-[320px]→h-80 (HeroSelection/FinanceOverview/NotificationsCenter/TeacherSessionTimeline/DashboardPage×2/TeachersPage/Dropdown), plus sticky mobile-header offsets top-[64px]→top-16 (AppointmentListView/AttendanceHistoryView); POST-state formal census = ZERO exact-key collisions app-side across all spacing families (w/h/min-*/max-h/insets/p/m), all 69 remaining sub-96px arbitraries are genuinely keyless (15/18/19/50/52/54/55/60/68/76/88/92px... - no default key equals them) and re-verified 9 text-[8px] sites as 100% badge/tooltip/24px-avatar-initial waivers (GREEN), 3 text-[28px] display numerals stay (font-dash doctrine); tokens h-60/h-80/min-w-40 already the pre-existing house pattern (Login/Reports/WhyChooseUs/MasarSection) = the fix raises consistency not novelty | A 'keep >96px' waiver is only valid when the value has NO default key - any exact key match is a phantom-tier violation no matter the size; the closure proof for spacing families = census mapped px→key with collision=0, and every remaining arbitrary must be keyless-by-definition (validated against the 37-row official dict, not by arithmetic) | tsc 0, 177/177 tests, vite build generates h-60/w-60/min-w-40/min-w-60/max-h-80/h-80/top-16, UTF8 round-trip clean on 9 files
| Sep 6 | Cross-family arbitrary-value residue pass - swept the remaining utility families after text/spacing/radius/duration closures (leading/tracking/rounded/border/ring/translate/rotate/scale/blur/size/inset): 102 arbitrary sites audited, 19 exact-token collisions converted - rounded-[4px]→rounded, rounded-[16px]→rounded-2xl (radius ladder), border-[1px]→border (native width 1px; dashed circles keep style+color utilities), negative hover nudges translate-x/y-[-2px]→-translate-x/y-0.5 (-2px) and translate-x-[-4px]→-translate-x-1 (-4px), centering translate-x-[-50%]→-translate-x-1/2, icon rasters size-[14/16/28px]→size-3.5/4/7 (spacing scale) across 8 files; consciously kept: blur-[50..140px] decorative blobs (53, no blur tokens exist), border-s/e-[3px] accent hairlines (9, off-ladder), rounded-[0.9rem/1.5rem/1.75rem/2rem/3rem/30%] non-ladder radii (15), leading-[1.1] editorial (unitless, between leading-none/tight), percent insets inset-[2%/4%]; NEW classes verified in dist CSS incl. negative hover variants + size-3.5 (CSS-escaped) | The phantom-tier rule closes across EVERY family: any arbitrary value that exactly equals a token (spacing/radius/translate-native/size/rounded/border-width) is a silent tier-split; unitless leading and percent radii are by-definition keyless keeps | tsc 0, 177/177 tests, vite build generates all new classes, UTF8 round-trip clean on 8 files
| Sep 6 | RTL logical-utility sweep pass 4 (arbitrary positional insets) - converted 22 physical right-/left- decorative/positioning insets to logical start-/end- (RTL-equivalent, zero visual shift): -right-[20%]/[2px]→-start-*, right-[-10%]/[30%]/[-40px]/[25%]→start-*, -left-[20%]→-end-, left-[-10%]/[20%]/[-30px]/[25%]→end-* (PublicFooter/PublicNavbar/PageLoader/AAbdullah/Blog/Courses/NotFound/HowItWorks), centered Courses hairline left-[50%]→start-1/2, full-width spans left-0 right-0→inset-x-0 (ScheduleGrid/StudentDrawer); verified KEEPS: 8 centered tooltips left-1/2 + -translate-x-1/2 (sanctioned), chat-bubble rounded-tr/tl-none = RTL-correct (bubble hugs inline-start in RTL), MobileSchedule divide-x divide-x-reverse = sanctioned RTL divider; tailwind.config mojibake = CONFIRMED CLEAN (restored in earlier round); POST-state: residual physical right-/left- arbitrary insets = ZERO | Right-side pinnings in RTL map to inline-START (start-*), left-side to inline-END (end-*) - a naive -right-[x]→-end-[x] global swap silently moves the element across the axis; physical arbitrary insets carry the same phantom-directional hazard as keyed left-4/right-4 | tsc 0, 177/177 tests, vite build generates all logical utilities (%-values escaped \\% in dist selectors), UTF8 round-trip clean on 10 files
| Sep 6 | RTL logical-utility sweep pass 5 + closure census - converted the last physical centering family left-1/2→start-1/2 (8 sites: tooltips on AttendanceStats/ParentsTable×2/StudentTable/TeacherTable + centered AAbdullah circle/NotFound blob, keeping -translate-x-1/2); then formal closure census across ALL horizontal-physical utilities (97 sites): text-left/right = 0, ml-/mr-/pl-/pr- = 0, PHYSICAL left-/right- in production tsx = 0 (only 4 JSX comments + 2 JS backgroundPosition props + 2 test titles remain); every other positive is a sanctioned/correct RTL pattern - avatars -space-x-N space-x-reverse (ParentsTable×2/QuranSection×2), MobileSchedule divide-x divide-x-reverse, AttendanceStats caret border-l-4 border-r-4 (symmetric diamond), DesktopLibraryLanding accent bars rounded-l-xl pinned end-0 (outer=rounded, inner=square = correct RTL), chat bubbles rounded-tr/tl-none (hug start), SidebarDesktop popover end-full + translate-x-1 (nudge toward item = correct in RTL), MotionSection hover:translate-x-2 (playground demo) + ~70 bg-gradient-to-r/l on decorative fills (color gradients do NOT mirror - kept as brand/decoration); guarded input backgroundPosition 'left 14px' stays JS-prop exempt | RTL closure invariant: a physical horizontal utility is only legal if it is symmetric (mx/px/border-l+r), the sanctioned overlap pairs (space-x-reverse/divide-x-reverse), corner hooks that are already logical-behavior-correct per edge, or decorative fills - CENTERED popovers must be logical start-1/2 (not left-1/2) so any future LTR page mirrors correctly; naive right→end / left→start swaps are wrong (RTL: right=start) | tsc 0, 177/177 tests, fresh vite build generates start-1/2, UTF8 round-trip clean on 6 files
| Sep 6 | Motion/transform-family arbitrary closure - audited scale-[*]/rotate-[*]/opacity-[*]/delay-[*]/duration-[*]/ease-[*] (185 sites): duration/delay/ease = 0 arbitrary (fully tokenized in v1.2.3); only 4 exact-native collisions converted - opacity-[0.5]→opacity-50 (ChatWindow glass dark-fallback stays dark:opacity-[0.08] which is genuinely keyless 8/100), active:scale-[0.95]→active:scale-95 (MobileQuickAccess), hover:scale-[0.95]→hover:scale-95 (EvaluationFormModal), group-hover:scale-[1.25]→group-hover:scale-125 (Courses image) with the rest-scale-1.15 kept (keyless 115); KEPT the micro-press ramp scale-[0.96..0.99/0.985/0.97/0.98] (148 sites, all keyless - native ladder jumps 95→[100,110,125]) as the deliberate tactile press palette across interactive rows/cards, plus rotate-[10deg] decorative + opacity-[0.01..0.08] sleeper texture fills (design choice below the 0.1 step); verified via native-scale probe set (0.9/1.05/1.1/1.25 and 0.4-0.6) = ZERO collisions remain | Same phantom-tier rule extends to transform/motion families: only scale-[0.95]/[1.25]/opacity-[0.5] hit a native key; the sub-0.1 opacity and 0.96-0.985 scale rasters and float-native-free rotate degrees are by-definition keyless (validated against the actual native default dict, not extrapolated) | tsc 0, 177/177 tests, vite build passes, UTF8 round-trip clean on 4 files
| Sep 6 | Transition specificity pass - audited all 688 transition-all sites with a strict non-color-prop guard (scale/rotate/translate/opacity/shadow/ring/blur/filters/size/insets/spacing/typography, applied to BOTH variants AND base classes) + a ±7-line window guard (catches multi-line cn() siblings and inline existing style= width triggers): only 13 provably color-only sites converted transition-all→transition-colors (SidebarDesktop tab, AttendanceHistoryModal×2 tabs, ChatSidebar close, ExecutiveAlerts severity chip, TransactionsLog×2 rows, ParentStudentCard row hover-border, CurrenciesSection filter chip, UsersSettings×2 status chips, Leads + TrialSessionFormModal filter chips); 4 of the 17 window-pure sites were SKIPPED after line inspection: FinancialSnapshot/TeacherAchievements/AcademicReport progress fills (width set via style={{width:...%}} - the documented progress-bar waiver, transition-all is REQUIRED to animate the fill) and shared Card base string (consumer sites merge hover:shadow-elevation classes OUTSIDE this file so its transition must stay all); post-state: transition-all = 675 all deliberate (fills/accordions/manual width hacks) | transition-all is the only transition that animates width/transform/shadow/opacity and multi-prop class flips; 'transition-colors' only covers background/color/border/fill/stroke - converting is SAFE only when NO non-color property can EVER change (checked on the whole ±7-line window, since className names can span cn() calls and inline style= ); progress fills that animate a style={{width}} MUST keep all | tsc 0, 177/177 tests, vite build generates transition-colors, UTF8 round-trip clean on 10 files
| Sep 6 | Blog keyboard-focus ring gap closure - 15 buttons in 4 files (SelectionGrid x10, BlogBreadcrumb x2, BlogCard x2, LoadMore x1) were ring-less: the earlier keyboard-focus ring pass explicitly excluded the blog package, and a deep-window project scan (337 candidates, 16-line prop window) confirmed every flag outside these 4 files is a FALSE positive (shared <Button> wrappers carry their own ring, test fixtures aren't DOM targets, or the ring lives beyond the scan window - DesktopLibraryLanding:646 and HeroSelection already carry the full recipe on their multi-line buttons); the 15 raw <button> tags got the sanctioned recipe 'outline-none focus-visible:ring-2 focus-visible:ring-focus' appended to their single-line className strings | A clickable element must show a keyboard ring in EVERY package - excluding a feature package from the focus-ring pass leaves a silent a11y hole (blog library's multi-step selection lets a Tab user step into grid buttons with zero visual feedback); the sanctioned recipe is the one ring signature (DesktopLibraryLanding's focus-visible:outline-none+focus-visible:ring-offset-2 variant is equivalent - both satisfy the ring contract) | tsc 0, 177/177 tests, vite build generates all three classes, UTF8 round-trip clean on 4 files, git diff = 15 clean +/- lines (autocrlf normalized) |
| Sep 6 | App-wide keyboard-focus ring closure - a tag-walking scanner (brace/quote/template-aware, skips <Button> components + test files, resolves className inside cn() and backtick templates, idempotent) found and fixed 260 raw <button> tags lacking a ring across 94 files: 198 pure-literal className strings + 62 JS/cn()/template expressions appended the sanctioned recipe 'outline-none focus-visible:ring-2 focus-visible:ring-focus' (single-line insert into the first literal arg); base-var families VERIFIED covered and untouched (actionBtnBase, FILTER_CHIP_CLASS, btnClass, Tabs styles.tab, Button variants map); the first scan pass was void (a stray continue skipped every lowercase <button>), and two codemod bugs were caught and reverted: an index bug (+10 vs +11) truncated the className= prefix to a bare c on all 198 literal edits (EndsWith guard rejected them, 75 files git-checkout-reverted, redone clean) and a backtick glue (recipe concatenated directly onto the next class token) fixed by a lookahead codemod across 15 sites; POST-STATE closure: full re-scan residual = 18, all provably covered by their base-var ring strings | Every raw <button> must show a keyboard ring regardless of how its className is assembled (literal, cn(), or template) - a var-family fix is authorized ONLY after reading the base consts ring token; index math on token prefixes (className= + quote = 11 chars, className= + brace = 11) is the #1 codemod landmine - guard with explicit prefix assertion before mutating | tsc 0, 177/177 tests, vite build generates the three ring classes, UTF8 round-trip clean on 94 files |
