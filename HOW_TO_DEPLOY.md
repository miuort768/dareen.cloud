# 🚀 خطوات الرفع وتفعيل الاستضافة

لقد قمت بتجهيز ملف الإعدادات لك. اتبع هذه الخطوات البسيطة عند الرفع:

## 1. رفع الملفات
ارفع مجلد المشروع بالكامل إلى الاستضافة.

## 2. إعداد ملف البيئة (.env)
1. اذهب إلى المجلد `server` الذي رفعته.
2. ستجد ملفاً اسمه `env_production_template.txt`.
3. قم بإعادة تسميته ليصبح `.env` (نقطة في البداية).
4. افتح الملف وعدّل السطر التالي برابط موقعك الحقيقي:
   ```
   FRONTEND_URL=https://اسم-موقعك.com
   ```

## 3. صلاحيات الكتابة (Permissions)
لكي يعمل حفظ البيانات وقاعدة البيانات (SQLite) بشكل صحيح، يجب أن تعطي "صلاحية كتاية" للمجلد.
1. في مدير الملفات (File Manager) في الاستضافة.
2. اضغط بالزر الأيمن على مجلد `server`.
3. اختر **Permissions** (أو Change Mode).
4. اجعلها **755** (أو **777** إذا واجهت مشاكل في الحفظ، لكن حاول 755 أولاً للأمان).
5. تأكد أن ملف `database.sqlite` داخله لديه صلاحيات قراءة وكتابة.

## 4. تشغيل التطبيق (Node.js)
في لوحة تحكم الاستضافة (cPanel مثلاً):
1. اذهب إلى **Setup Node.js App**.
2. أنشئ تطبيق جديد.
3. حدد مسار التطبيق (Application root) ليكون مجلد `server`.
4. حدد ملف البدء (Application startup file) ليكون `index.js`.
5. اضغط **Start App**.

مبروك! موقعك يعمل الآن. 🎉
